# مراقي - ملاحظات التنفيذ

## ما تم تنفيذه

تم بناء Prototype تفاعلي عربي RTL لتطبيق مراقي، مع لوحة إدارة وصفحات مبدئية لإدارة المشاريع والتقارير اليومية.

### Sidebar

- Sidebar ثابت على يمين الشاشة.
- تصميم Light Mode بخلفية بيضاء وحدود خفيفة.
- لون الحالة النشطة أخضر `#2E8B57`.
- روابط عربية مع Angular Router.
- أيقونات PrimeIcons لكل عنصر.
- زر لتصغير الـSidebar إلى شريط أيقونات بعرض `72px`.
- عند التصغير تختفي النصوص وتظل الأيقونات ظاهرة.
- عند التوسيع تظهر الأيقونات والنصوص مرة أخرى.
- على الشاشات الصغيرة يعمل كـoffcanvas.
- تمت إضافة حالات focus وخصائص accessibility مثل `aria-label` و`aria-expanded`.

### عناصر التنقل

- الرئيسية
- المشاريع
- التقارير
- المشاكل
- الاحتياجات
- المهندسون
- التقارير التحليلية
- الإعدادات
- تسجيل خروج

## الصفحات الحالية

### Dashboard

صفحة الرئيسية تحتوي على:

- عدد المشاريع النشطة.
- تقارير اليوم.
- التقارير المتأخرة.
- المشاكل المفتوحة.
- ملخص حالة المواقع.
- رسم بياني مبدئي للالتزام الأسبوعي.
- جدول آخر التقارير.

### صفحات الإدارة

تمت إضافة واجهة مبدئية لكل صفحة:

- المشاريع: إجمالي المشاريع، المشاريع الطبيعية، التي تحتاج متابعة، والمعطلة.
- التقارير اليومية: التقارير المرسلة، المتأخرة، والمسودات.
- المشاكل: المشاكل المفتوحة، قيد المتابعة، والمحلولة.
- الاحتياجات: الطلبات المعلقة، التي تم توفيرها، وعالية الأولوية.
- المهندسون: عدد المهندسين، نسبة الالتزام، والتقارير الناقصة.
- التقارير التحليلية: مؤشرات الأداء ورسم بياني أسبوعي.
- الإعدادات: المستخدمون، المشاريع، الإشعارات، وحالة النظام.

## الهيكل البرمجي

- `src/app/components/navbar/`: الـSidebar والتنقل الرئيسي.
- `src/app/components/home/`: Dashboard الرئيسية.
- `src/app/components/workspace-page/`: صفحة الإدارة القابلة لإعادة الاستخدام.
- `src/app/interfaces/nav-item.ts`: تعريف عناصر التنقل.
- `src/app/app.routes.ts`: تعريف routes والبيانات المبدئية لكل صفحة.
- `src/app/app.css`: تخطيط الصفحة والمساحة المتغيرة بجانب الـSidebar.
- `src/styles.css`: التنسيقات العامة واتجاه RTL.

## الأيقونات

تم استخدام PrimeIcons من خلال classes مثل:

```html
<i class="pi pi-home"></i>
<i class="pi pi-building"></i>
<i class="pi pi-file"></i>
```

تم إضافة `primeicons` إلى `package.json`، كما تم تحميل stylesheet الخاص بها في `src/index.html` لضمان ظهور الأيقونات.

## البيانات الحالية

الأرقام والتقارير الحالية Mock Data مخصصة للـPrototype فقط. لم يتم ربط Backend أو قاعدة بيانات حتى الآن.

## الربط المستقبلي مع Backend

سيتم استبدال البيانات الموجودة في `app.routes.ts` بخدمات Angular تتصل بـAPI حقيقي، مثل:

- `GET /projects`
- `GET /reports`
- `GET /issues`
- `GET /needs`
- `GET /engineers`
- `GET /analytics`

كما سيتم ربط الرسوم البيانية بنتائج التقارير الفعلية، وإضافة:

- حالات التحميل.
- حالات عدم وجود بيانات.
- معالجة الأخطاء.
- الفلاتر والتواريخ.
- الصلاحيات وتسجيل الدخول.

## ملاحظات مهمة

- المشروع حاليًا Prototype وليس نظامًا متصلًا ببيانات حقيقية.
- روابط الصفحات تعمل من خلال Angular Router.
- يمكن استبدال الـMock Data لاحقًا دون إعادة بناء تصميم الواجهات.
- التصميم متوافق مع اتجاه التطبيق العربي RTL.
